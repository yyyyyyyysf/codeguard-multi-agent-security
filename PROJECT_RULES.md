# PROJECT_RULES.md — CodeGuard 项目唯一权威开发规范

> **Single Source of Truth**  
> 本文件为项目唯一权威开发规范。所有开发、迭代、重构、提交、文档编写行为均需严格遵守本规则。  
> 规则变更仅允许追加更新，禁止覆盖、删除历史原则，保障项目全生命周期规范统一、上下文可追溯、开发可复现。

---

## 目录

1. [项目目标](#一项目目标)
2. [开发原则](#二开发原则)
3. [开发流程](#三开发流程)
4. [模块开发规则](#四模块开发规则)
5. [架构规范](#五架构规范)
6. [代码规范](#六代码规范)
7. [测试规范](#七测试规范)
8. [文档规范](#八文档规范)
9. [Git规范](#九git规范)
10. [ADR规范](#十adr规范)
11. [重构规范](#十一重构规范)
12. [项目专项规范](#十二项目专项规范)
13. [违规处罚与修正机制](#十三违规处罚与修正机制)
14. [规则维护机制](#十四规则维护机制)
15. [项目技术决策速查](#十五项目技术决策速查)

---

## 一、项目目标

### 1.1 项目定位

CodeGuard 是一个**多 Agent 协作的代码库智能分析平台**，以 API + Webhook 为核心交付形态，嵌入 CI/CD 研发工作流，在 PR 提交、依赖变更等风险发生点自动拦截安全问题与迁移风险。

### 1.2 核心功能

| 功能模块 | 说明 |
|----------|------|
| 安全审计 | 依赖 CVE 扫描（三级缓存）+ 代码安全规则扫描（Semgrep），零 LLM、纯规则引擎，证据链 100% 可追溯 |
| 冲突消解 | 自动裁决引擎（豁免规则）+ 人在回路（争议项人工兜底），输出阻断/放行决策，直接控制 PR 合并状态 |
| 迁移评估 | Breaking Changes 规则库 + LLM 辅助代码影响分析，评估框架/依赖版本升级风险与工作量 |
| 报告聚合 | 分级输出策略（安全结果优先回写 PR + 迁移结果异步追加），模板引擎渲染，不承载业务判断 |

### 1.3 技术栈

| 层级 | 技术选型 |
|------|----------|
| LLM 与推理 | 安全：纯规则引擎（零 LLM）；迁移：DeepSeek-Coder / Qwen2-Code；聚合：轻量模型 + Jinja2 模板 |
| Agent 编排 | LangGraph（StateGraph DAG 主链路 + 独立子工作流，原生支持人在回路 Checkpoint） |
| 后端服务 | FastAPI + Celery + Redis（异步 API + 任务队列 + 事件总线 + 缓存） |
| 数据与规则 | OSV API + GitHub Advisory Database + 本地 SQLite 热点缓存 + Semgrep 规则 |
| 代码解析 | Tree-sitter + 依赖文件解析器 |
| 交付形态 | GitHub Webhook（核心）+ CLI 工具（辅助）+ 极简 Web 后台（辅助） |
| 存储 | Redis（热数据/缓存/队列）+ SQLite（持久化/MVP）+ 本地文件系统（报告） |
| 部署 | Docker Compose |

---

## 二、开发原则

### 2.1 正确性优先

- 功能正确性是第一优先级，安全链路的每条结论必须有权威数据源支撑
- 安全审计不使用 LLM，所有结论来自 OSV/GitHub Advisory API + Semgrep 确定性规则
- 降级场景下，宁可多报 warning，绝不漏掉 blocking

### 2.2 可维护性优先

- 代码简洁清晰，模块职责单一，依赖单向
- 文档与代码实时同步，新开发者可通过文档完整还原开发上下文
- 架构决策必须写入 ADR，永久留存

### 2.3 小步迭代

- 单次迭代聚焦单一模块/单一功能，完成完整闭环后再开启下一迭代
- MVP 先跑通核心闭环（安全审计 → 冲突消解 → PR 回写），再逐步追加辅助链路

### 2.4 单模块开发

- 一次只能开发一个模块，模块完成后方可进入下一模块
- 禁止同时开发多个模块、禁止边设计边开发

### 2.5 禁止 Vibe Coding

- 所有代码编写前必须有明确的设计文档或 ADR
- 禁止无设计随意编码、禁止凭感觉堆叠代码
- 每个技术选型必须能讲出对比方案和选择理由

### 2.6 禁止一次生成整个项目

- 按模块顺序逐个开发，禁止一次性生成全量代码
- 每个模块走完「分析 → 设计 → 实现 → 测试 → 文档 → 提交」全流程

### 2.7 禁止跳步开发

- 严格遵循开发流程，禁止跳过需求分析直接编码
- 禁止跳过测试直接提交，禁止跳过文档更新直接进入下一模块

---

## 三、开发流程

本项目开发流程为**强制串行流程**，任何场景下禁止跳步、并行、倒序开发。单模块全流程走完方可进入下一模块。

### 3.1 需求分析阶段

1. 通读项目大纲、历史 ADR 文档、ARCHITECTURE.md，明确模块核心需求、业务目标、边界范围
2. 梳理模块输入、输出、依赖模块、外部依赖、约束条件、异常场景
3. 输出模块需求分析小结，明确本次开发范围，排除范围外功能
4. 识别潜在需求风险、技术风险，提前记录备案

### 3.2 架构设计阶段

1. 基于项目整体架构规范，完成单模块分层设计、接口设计、数据结构设计、数据流设计
2. 确认模块依赖关系，避免循环依赖、冗余依赖
3. 如需调整整体架构、新增技术方案，必须编写对应 ADR 文档
4. 设计完成后同步更新 ARCHITECTURE.md 对应模块内容

### 3.3 代码实现阶段

1. 严格按照设计方案、代码规范进行编码，禁止边写边改设计
2. 聚焦当前模块，不提前开发后续模块功能
3. 实时自检代码规范、逻辑正确性，及时清理冗余代码
4. 遇到设计缺陷、需求模糊问题，立即暂停开发，记录问题并等待确认

### 3.4 测试验证阶段

1. 优先编写单元测试，覆盖核心业务逻辑、分支逻辑、边界条件、异常场景
2. 完成集成测试，验证模块间调用、数据流转的正确性
3. 手动验收全量功能，确保与需求一致
4. 记录测试用例、测试结果、问题修复过程，同步写入 DEVELOPMENT_LOG

### 3.5 文档更新阶段

1. 同步更新模块 README、ARCHITECTURE.md、DEVELOPMENT_LOG
2. 新增功能、接口、配置、部署方式需完整写入文档
3. 如有方案变更、架构调整，追加更新 ADR 文档

### 3.6 Git 提交阶段

1. 代码、测试、文档全部完成后，统一整理代码变更
2. 按照 Git 规范编写提交信息，单次模块开发对应单次规范提交
3. 提交前自检代码规范、文件完整性、文档完整性

### 3.7 等待确认阶段

1. 提交完成后，暂停所有开发工作，等待审核确认
2. 根据反馈进行微调修复，修复后重新走完测试、文档、提交流程

---

## 四、模块开发规则

### 4.1 单模块开发原则

- **一次只能开发一个模块**，模块完成后方可进入下一模块
- **禁止同时开发多个模块**，禁止在模块 A 未完成时启动模块 B
- 模块完成判定标准见下方 4.2

### 4.2 模块完成标准（全部满足方可判定完成）

| # | 标准 | 说明 |
|---|------|------|
| 1 | 功能完成 | 模块所有需求点 100% 实现，无遗漏、无阉割、无临时硬编码兜底方案 |
| 2 | 测试通过 | 单元测试 + 集成测试通过率 100%，无报错、无告警、无未处理异常 |
| 3 | README 更新 | 补充模块功能说明、使用方式、入参出参、依赖说明、注意事项 |
| 4 | DEVELOPMENT_LOG 更新 | 记录开发时间、开发内容、核心改动、遗留问题、测试结果、版本节点 |
| 5 | ARCHITECTURE 更新 | 同步更新架构文档，补充模块架构图、职责、依赖关系、数据流、接口定义 |
| 6 | 规范 Commit | 按照 Git 规范生成标准化提交信息，清晰记录改动内容、目的 |
| 7 | 代码规范自检 | 无格式错误、无冗余代码、无废弃代码、无违规写法、无硬编码敏感信息 |
| 8 | 无编译/运行异常 | 项目可正常编译、启动、运行，无控制台报错、无致命告警 |
| 9 | 依赖闭环 | 依赖已完整引入、版本锁定，无缺失、无冲突、无冗余 |
| 10 | 可交付闭环 | 模块具备独立可运行、可测试、可使用能力，无需后续二次修改 |

### 4.3 推荐模块开发顺序

```
Phase 1: 基础设施
  1. 项目骨架搭建（目录结构、pyproject.toml、Docker Compose、配置管理）
  2. core 层（异常体系、通用数据模型、常量）
  3. utils 层（Git 操作、签名工具、重试、日志、ID 生成）
  4. storage 层（CVE 缓存、OSV 客户端、审计日志、规则存储、报告存储接口）

Phase 2: 预处理 + 引擎
  5. preprocess 模块（仓库克隆、依赖解析、Tree-sitter AST、CodeMetadata 组装）
  6. engine 层（编排引擎、事件总线 Redis Pub/Sub）

Phase 3: Agent 核心（按依赖顺序）
  7. 安全审计 Agent（CVE 扫描 + Semgrep 代码规则）
  8. 冲突消解 Agent（自动裁决引擎 + 人在回路）
  9. 安全主链路联调（Agent ①→② DAG 跑通）
  10. 迁移评估 Agent（Breaking Changes 规则库 + LLM 辅助分析）
  11. 报告聚合 Agent（模板渲染 + 分级输出策略）

Phase 4: 接入层
  12. integrations 层（GitHub Client、Webhook Sender）
  13. FastAPI 接口层（API 路由 + Webhook 接收 + 中间件）
  14. Celery 任务层（异步任务编排 + 回调）

Phase 5: 交付与测试
  15. CLI 工具
  16. Web 管理后台（极简版）
  17. 全链路集成测试 + 降级测试 + 安全测试
  18. Docker Compose 一键部署验证
```

---

## 五、架构规范

### 5.1 分层架构

```
src/
├── core/           # 核心公共层：异常体系、通用数据模型、常量（全项目依赖）
├── utils/          # 工具层：Git、签名、重试、日志、ID生成、沙箱（全项目依赖）
├── storage/        # 数据与存储层：CVE缓存、OSV客户端、审计日志、规则存储、报告存储
├── preprocess/     # 预处理模块（非Agent，纯确定性）：仓库解析、依赖提取、AST生成
├── engine/         # 引擎层：编排引擎、事件总线（依赖Agent抽象基类，不依赖具体实现）
├── agents/         # Agent层：安全审计、冲突消解、迁移评估、报告聚合
├── integrations/   # 外部集成层：GitHub Client、GitLab Client、Webhook Sender
├── api/            # FastAPI接口层：路由、中间件、请求/响应Schema
├── webhook/        # Webhook处理层：接收、签名校验、事件分发
├── tasks/          # Celery异步任务层
├── monitoring/     # 可观测性层：指标埋点、链路追踪
├── cli/            # CLI工具
└── web/            # Web管理后台
```

### 5.2 依赖规则

- **依赖单向**：上层依赖下层，禁止反向依赖，禁止循环依赖
- **核心层全域可依赖**：`src/core/*` 和 `src/utils/*` 为全项目公共依赖
- **Agent 不直接调外部 API**：所有外部系统交互必须走 `integrations/` 层
- **引擎层依赖倒置**：引擎只依赖 Agent 抽象基类 `BaseAgent`，不依赖具体 Agent 实现；具体 Agent 主动向引擎注册、订阅事件
- **接口与实现分离**：核心能力通过接口暴露，内部实现封装隐藏，外部仅依赖接口

### 5.3 关键架构约束

| 约束 | 说明 |
|------|------|
| 安全主链路独立 | 安全审计 → 冲突消解 为严格 DAG，迁移评估输出永不进入安全决策链路 |
| 安全结果前置阻断 | 冲突消解 Agent 做出决策后，通过 integrations 层直接执行 PR 阻断，不经过报告聚合中转 |
| 预处理非 Agent | 预处理模块不使用 LLM，纯确定性逻辑，输出标准化 CodeMetadata 作为所有 Agent 统一输入 |
| 混合编排 | 安全主链路用 LangGraph StateGraph 严格 DAG；辅助链路用 Redis Pub/Sub 事件驱动 |
| 分级输出 | 安全结果就绪 → 立即回写 PR（精简版）；迁移结果就绪 → 异步追加；全量就绪 → 生成完整报告 |
| 降级不静默 | 所有降级场景输出必须携带 `degraded: true` + `degraded_reason`，禁止无感知静默降级 |

### 5.4 轻量化原则

- 拒绝过度设计，在满足可维护、可扩展前提下保持架构简洁
- MVP 阶段 SQLite 够用，不强上 PostgreSQL
- 架构预留扩展接口但不提前实现，按需迭代

---

## 六、代码规范

### 6.1 命名规范

- 变量、函数、类、文件、目录命名语义化、见名知意
- Python 代码遵循 PEP 8：类用 PascalCase，函数/变量用 snake_case，常量用 UPPER_SNAKE_CASE
- 禁止拼音命名、无意义简写、混乱大小写

### 6.2 格式统一

- 使用 Ruff 或 Black 统一代码格式化
- 缩进：4 空格
- 行宽：100 字符
- 文件末尾保留一个空行

### 6.3 逻辑规范

- 代码逻辑简洁清晰，避免嵌套过深（不超过 3 层）
- 重复代码统一抽离公共方法/工具类
- 函数、类、模块严格遵循单一职责原则

### 6.4 注释规范

- 核心逻辑、复杂算法、特殊处理必须添加注释
- 公共接口/函数必须有 docstring（Google 风格）
- 禁止无注释、无效注释、过期注释

### 6.5 类型注解

- 所有公共函数/方法必须添加类型注解
- 使用 Pydantic 进行数据模型定义与校验

### 6.6 异常处理

- 所有可预判异常必须主动捕获、处理、日志记录
- 使用 `src/core/errors.py` 中定义的统一异常体系
- 禁止直接抛出原始异常、禁止忽略异常（裸 except）

### 6.7 安全性规范

- 禁止硬编码密钥、账号、密码、Token、路径等敏感信息
- 敏感数据统一通过环境变量/配置文件读取
- 报告中发现的密钥/Token 默认脱敏展示（如 `sk-***abcd`）

### 6.8 依赖管理

- 所有依赖在 `pyproject.toml` 中声明，版本锁定
- 禁止引入未经评估的第三方库
- 开发完成后清理未使用的依赖

---

## 七、测试规范

### 7.1 测试分层

| 层级 | 覆盖目标 | 工具 |
|------|----------|------|
| 单元测试 | 每个 Agent 核心逻辑、自动裁决引擎规则分支、数据模型、预处理解析器 | pytest |
| 集成测试 | API 端点、Webhook 签名校验、数据库/Redis 读写、Celery 任务 | pytest + httpx |
| 端到端测试 | 完整分析链路（真实仓库 → 安全扫描 → 冲突消解 → 报告输出） | Docker Compose |
| 降级测试 | OSV/Redis/LLM 不可用时的降级行为 | Mock 外部依赖 |
| 安全测试 | Webhook 伪造签名拒绝、回调签名验证、SQL 注入、审计日志不可篡改 | pytest |
| Benchmark | CVE 查询延迟、安全主链路端到端延迟、迁移评估耗时 | pytest-benchmark |

### 7.2 测试要求

- 核心功能优先编写测试用例，再完成业务代码（TDD 可选，但测试不可省略）
- 关键业务代码测试覆盖率 ≥ 70%
- 所有测试用例可重复执行，结果稳定一致
- 测试发现的问题必须全部修复、复测通过，禁止带病提交
- 所有测试用例、脚本、记录统一留存 `tests/` 目录

### 7.3 Benchmark SLA

- 安全关键路径延迟（Webhook 接收 → 安全扫描 → 冲突消解 → PR 状态更新）：**≤ 3s**
- 全量分析总延迟（含迁移评估、完整报告生成）：**≤ 5s**

---

## 八、文档规范

### 8.1 核心原则

- **实时同步**：文档与代码、架构、功能变更实时同步，代码改、文档必改
- **完整可追溯**：所有设计、改动、优化、问题解决过程全部记录归档
- **仅追加更新**：历史内容不删除、不覆盖，保留全生命周期变更轨迹

### 8.2 必填文档清单

| 文档 | 用途 | 更新时机 |
|------|------|----------|
| PROJECT_RULES.md | 项目唯一权威开发规范 | 规则变更时追加 |
| ARCHITECTURE.md | 系统架构文档（分层、模块、数据流、接口） | 每个模块完成后 |
| DEVELOPMENT_LOG.md | 开发日志（时间、内容、改动、问题、测试结果） | 每个模块完成后 |
| 各模块 README.md | 模块功能说明、使用方式、入参出参、依赖 | 每个模块完成后 |
| ADR/*.md | 架构决策记录 | 架构/技术方案变更时 |

### 8.3 格式规范

- 所有文档统一使用标准 Markdown 格式
- 排版整洁、层级清晰（最多 4 级标题）
- 代码块标注语言类型
- 表格对齐、表头加粗

---

## 九、Git 规范

### 9.1 分支策略

- `main`：主干分支，保持稳定可交付
- `feature/<module-name>`：功能开发分支，每个模块独立分支
- 禁止直接提交 `main` 分支

### 9.2 Commit Message 规范

格式：`【类型】模块名：改动说明`

| 类型 | 说明 |
|------|------|
| feat | 新增功能 |
| fix | 问题修复 |
| refactor | 重构（不改变功能） |
| docs | 文档更新 |
| test | 测试新增/修改 |
| chore | 构建/配置/依赖变更 |

示例：`【feat】security-agent：实现 CVE 三级缓存扫描，支持 CVE 阻断分级`

### 9.3 提交规则

- 单次提交对应单一功能、单一模块，禁止多功能堆叠
- 禁止提交：无效文件、缓存文件、日志文件、敏感文件、本地配置、编译产物
- 提交前自检：代码规范、测试结果、文档完整性
- `.gitignore` 确保排除 `.env`、`__pycache__`、`.pytest_cache`、`*.pyc`、`node_modules/`

---

## 十、ADR 规范

### 10.1 触发条件

涉及以下变更时，必须新增/更新 ADR：
- 架构调整（分层、模块拆分/合并、依赖关系变更）
- 技术选型或替换
- 核心逻辑重构
- 安全策略/规则变更
- 数据模型重大变更

### 10.2 文档格式

- 统一存放于 `ADR/` 目录
- 命名：`ADR-<序号>-<主题>.md`（如 `ADR-001-security-agent-no-llm.md`）
- 必填内容：问题背景、现状痛点、可选方案、方案对比、最终决策、落地影响、实施步骤、遗留问题

### 10.3 决策流程

1. 编写 ADR 草案 → 2. 评审讨论 → 3. 审批通过 → 4. 落地代码 → 5. 归档留存

**禁止先开发后补 ADR。**

---

## 十一、重构规范

### 11.1 核心原则

- **重构不改功能**：只优化代码结构，不改动业务功能
- **小步重构**：单次重构范围最小化，避免大规模一次性重构
- **重构必测试**：重构完成后全量回归测试，保证功能 100% 一致

### 11.2 重构流程

1. 编写重构方案 + ADR（大规模/核心模块重构）
2. 评审通过
3. 执行重构
4. 全量回归测试
5. 更新 ARCHITECTURE.md
6. 记录 DEVELOPMENT_LOG

### 11.3 禁止无效重构

- 禁止无意义的格式微调、命名微调
- 仅针对：代码冗余、结构混乱、设计缺陷、性能问题进行定向重构

---

## 十二、项目专项规范

### 12.1 环境规范

- 开发、测试、生产环境严格隔离
- 环境配置通过 `.env` 文件管理，`.env.example` 作为模板
- 禁止开发环境配置污染生产环境

### 12.2 安全规范（CodeGuard 专项）

- **安全主链路零 LLM**：安全审计 Agent 禁止引入任何 LLM 调用，所有结论来自确定性规则 + 权威数据源
- **证据链溯源**：每个安全结论必须绑定来源（OSV/GitHub Advisory/Semgrep 规则 ID）
- **阻断优先于报告**：安全结果就绪后立即执行阻断/放行，报告生成异步进行
- **fail-safe**：外部依赖故障时，安全决策默认保守（维持 blocking），不自动放行
- **审计不可篡改**：审计日志 append-only + 链式哈希（`row_hash = SHA256(prev_hash + current_row)`）
- **人在回路超时兜底**：人工裁决等待超时（默认 24h），自动维持 blocking=true
- **降级不静默**：所有降级输出必须标记 `degraded: true` + `degraded_reason`

### 12.3 性能规范

- CVE 查询三级缓存：Redis 热点 → SQLite 本地 → OSV API 回源
- 安全主链路延迟目标 ≤ 3s
- 核心逻辑避免循环冗余计算、重复查询

### 12.4 兼容性规范

- 迭代开发保证向下兼容，新版本不破坏旧版本能力
- API 接口、数据模型、配置文件兼容历史版本

### 12.5 可观测性规范

- 核心流程、关键节点保留结构化日志
- 异常场景完整日志输出（traceback + 上下文）
- 监控指标埋点（scan_request_total、scan_duration、blocking_rate、cache_hit_rate 等）

### 12.6 合规规范

- 项目代码、资源、文档无侵权、无违规内容
- 所有自研内容可自主可控
- 不使用未授权的外部 API/服务

---

## 十三、违规处罚与修正机制

1. 出现跳步开发、多模块并行、无设计编码、无测试提交、文档滞后等违规行为，必须**立即暂停开发**，整改完毕后方可继续
2. 违规代码、违规提交一律退回重改，不允许带入后续迭代
3. 所有违规情况、整改过程需记录在 DEVELOPMENT_LOG 中，作为项目迭代复盘依据

---

## 十四、规则维护机制

1. 本规则为项目最高权威规范，永久有效，所有后续迭代、新增需求、架构升级均需遵守
2. 规则更新**仅支持追加**：新增章节、补充细则，禁止修改、删除、覆盖历史已确立的原则
3. 规则新增后同步归档，保证新旧规则完整可查，全生命周期可追溯
4. 每次规则变更需在 DEVELOPMENT_LOG 中记录变更原因、变更内容、变更时间

---

## 十五、项目技术决策速查

> 以下为项目设计阶段已确定的核心技术决策，详细论证见各 ADR 文档。此处作为快速索引，开发时如有疑问先查此处。

| 决策项 | 选择 | 核心理由 |
|--------|------|----------|
| 安全审计不使用 LLM | 纯规则引擎 + OSV API + Semgrep | 合规要求（安全结论不能由黑盒模型生成）、零幻觉、证据链可追溯 |
| 编排框架 | LangGraph | 原生 StateGraph + Checkpoint 支持人在回路，工业界主流标准 |
| 混合编排模式 | 安全 DAG + 辅助事件驱动 | 安全链路确定性 + 辅助链路扩展性，互不干扰 |
| 后端框架 | FastAPI + Celery + Redis | 异步原生支持、任务队列解耦、Redis 复用为事件总线 |
| CVE 数据源 | 三级缓存（Redis→SQLite→OSV API） | 保障 3s SLA，冷热分层，避免 API 限流 |
| 交互形态 | API+Webhook 为核心 | 嵌入 CI/CD 工作流，在风险发生点自动拦截（非事后报告） |
| 人在回路位置 | 冲突消解 Agent 内部 | 仅在规则无法裁决时触发，不打断主链路自动化流程 |
| PR 状态更新 | GitHub Check Runs API | 原生支持通过/失败/进行中状态，与 Actions 体验一致 |
| 审计日志防篡改 | append-only + 链式哈希 | 满足等保 2.0 审计日志合规要求 |
| 报告分级输出 | 安全结果优先回写 + 迁移结果异步追加 | 兼顾阻断 SLA 与分析完整性 |
| 预处理非 Agent | 纯确定性工具模块 | 避免 LLM 概念泛化，数据源统一，避免 Agent 间数据不一致 |
| 降级策略 | fail-safe 保守原则 | 外部依赖故障时，安全决策维持 blocking，不自动放行 |

---

## 十六、安全链路专项规范

> 本章为 CodeGuard 作为安全产品的核心合规底线，优先级与第二章「开发原则」并列，任何开发行为不得违反。

### 16.1 安全主链路不可旁路原则

- 安全审计 → 冲突消解 → 阻断执行的链路为系统最高优先级
- 任何模块故障、降级场景都不得绕过安全判断
- 宁误报不漏报：高危风险（CVSS ≥ 7.0）默认阻断，不依赖任何外部服务可用性
- 安全链路的执行优先级高于所有辅助分析链路，独占计算资源保障 SLA

### 16.2 零 LLM 原则（安全域）

- **安全审计 Agent**：核心判断逻辑必须基于规则引擎（Semgrep）、权威漏洞库（OSV/GitHub Advisory），**禁止引入任何 LLM 调用**
- **冲突消解 Agent**：自动裁决基于确定性豁免规则匹配，**禁止使用 LLM 做安全决策**
- **红线**：安全结论绝对不能由黑盒模型生成，违反此条即项目核心价值失效

### 16.3 证据链强制留存

- 所有安全结论必须绑定权威来源：
  - 漏洞类：CVE 编号 + OSV/GitHub Advisory 链接 + 修复版本
  - 代码规则类：Semgrep 规则 ID + 命中代码行号 + 代码片段
- 审计日志采用 append-only + 链式哈希（`row_hash = SHA256(prev_hash + current_row)`），满足等保 2.0 / 金融行业合规审计的防篡改要求
- 每次扫描记录规则库版本号（`rule_set_version`）和漏洞库版本号（`cve_db_version`），保证扫描结果 100% 可复现

### 16.4 人工裁决边界

- **触发条件**：仅规则库未覆盖的边缘场景可触发人工干预
- **硬性限制**：
  - Critical 级漏洞（CVSS ≥ 9.0）**禁止人工豁免**，必须修复
  - 人工豁免必须填写豁免理由、留存操作人信息、上传证据链接
  - 人工裁决超时（默认 24h）自动维持 `blocking=true`，不自动放行
- **审计追溯**：所有人工操作记录操作人 ID、操作时间、豁免理由、证据链接，写入审计日志

---

## 十七、Agent 开发专项规范

> 本章对齐多 Agent 系统的工程化最佳实践，所有 Agent 开发必须遵守。

### 17.1 契约强制

- 每个 Agent 必须明确定义：
  - **输入 Schema**：接收的数据结构（Pydantic 模型），字段类型、必填/可选、约束条件
  - **输出 Schema**：产出的数据结构（Pydantic 模型），字段类型、含义说明
  - **职责边界**：一句话描述该 Agent 做什么、不做什么
  - **失败策略**：至少 1 种降级方案，外部依赖故障时的行为定义
- Agent 之间**禁止直接调用内部逻辑**，所有交互通过事件总线（Redis Pub/Sub）或编排层（LangGraph StateGraph）流转
- 每个 Agent 继承 `BaseAgent` 抽象基类，向引擎层注册自己的事件订阅

### 17.2 职责单一

- 禁止 Agent 承担超出自身定位的职责，具体边界如下：

| Agent | 可以做 | 禁止做 |
|-------|--------|--------|
| 安全审计 | CVE 扫描、代码规则扫描、输出 SecurityReport | 修改安全结论、执行阻断、调用 LLM |
| 冲突消解 | 规则匹配裁决、人工裁决调度、输出阻断/放行指令 | 修改原始安全发现、调用 LLM、直接操作 GitHub API |
| 迁移评估 | Breaking Changes 匹配、LLM 辅助代码影响分析 | 参与安全决策、修改安全结论、直接操作外部 API |
| 报告聚合 | 结果去重合并、模板渲染、风险分级排序 | 修改安全结论、修改迁移评估结果、直接操作外部 API |

### 17.3 无状态原则

- 所有 Agent 本身**不持久化业务数据**
- 状态统一由编排层（LangGraph Checkpoint）和存储层（Redis/数据库）管理
- 保证 Agent 可横向扩展、可重复执行、可独立测试

### 17.4 降级约定

- 每个 Agent 必须定义**至少 1 种降级策略**
- 外部依赖故障时自动切换降级模式
- 降级输出必须标记 `degraded: true` + `degraded_reason`（具体降级原因）
- **禁止静默失败**：降级不可无感知，必须在日志和输出中显式标注

---

## 十八、供应链与依赖安全规范

> 作为安全产品，CodeGuard 自身的安全合规是基本前提。

### 18.1 依赖锁定

- 所有第三方依赖必须锁定版本（`pyproject.toml` 精确版本或 `poetry.lock`）
- 禁止使用 `latest`、`>=` 无上界等浮动版本标签
- 引入新依赖前必须：
  1. 评估许可证兼容性
  2. 通过 `pip-audit` 或 OSV 漏洞扫描
  3. 在 DEVELOPMENT_LOG 中记录引入原因

### 18.2 凭证管理

- 敏感凭证、API 密钥、Webhook Secret **全部通过环境变量注入**
- `.env` 文件必须加入 `.gitignore`
- `.env.example` 提供模板（仅保留 Key 名，Value 为空或占位符）
- 代码中禁止硬编码任何形式的密钥、Token、密码
- 报告中发现的密钥/Token 默认脱敏展示（如 `sk-***abcd`），完整值仅对仓库 Owner 可见

### 18.3 自举验证

- 项目自身的代码提交必须能触发内置的安全扫描
- 在 CI/CD 中配置：每次 PR 提交 → CodeGuard 自身扫描 → 通过后方可合并
- 实现「用 CodeGuard 保障 CodeGuard 开发」的自举闭环

---

## 十九、文档目录结构规范

> 统一项目文档归档路径，保持根目录整洁。

### 19.1 目录分工

```
codeguard/                          # 项目根目录
├── PROJECT_RULES.md                # 唯一权威开发规范（最高优先级）
├── ARCHITECTURE.md                 # 系统架构总览（快速查阅入口）
├── DEVELOPMENT_LOG.md              # 开发日志（全生命周期记录）
├── README.md                       # 项目介绍与快速开始
├── pyproject.toml                  # 项目元数据与依赖
├── docker-compose.yml              # 容器化部署
├── Dockerfile                      # 容器镜像
├── .env.example                    # 环境变量模板
├── .gitignore                      # Git 忽略规则
│
├── docs/
│   ├── specs/                      # 设计规格文档（正式设计依据）
│   │   └── codeguard-v0.1-design-spec.md
│   ├── adr/                        # 架构决策记录
│   │   ├── ADR-001-security-agent-no-llm.md
│   │   └── ...
│   └── guides/                     # 开发指南与手册
│       ├── setup-guide.md
│       └── api-reference.md
│
├── src/                            # 源代码（详见 ARCHITECTURE.md）
├── tests/                          # 测试代码与固件
├── config/                         # 静态配置文件（规则、Semgrep配置等）
├── cli/                            # CLI 工具入口（或移入 src/cli/）
└── web/                            # Web 管理后台（或移入 src/web/）
```

### 19.2 文档优先级

| 优先级 | 文档 | 说明 |
|--------|------|------|
| P0 | PROJECT_RULES.md | 开发前必读，所有行为以此为准 |
| P0 | ARCHITECTURE.md | 系统骨架，理解项目全貌 |
| P1 | docs/specs/*.md | 当前版本正式设计规格 |
| P1 | docs/adr/*.md | 架构决策历史上下文 |
| P2 | docs/guides/*.md | 开发指南、环境搭建、API 参考 |

---

## 二十、接口与兼容性规范

> 面向后续迭代的边界约定，保障系统平滑演进不破坏现有能力。

### 20.1 API 版本化

- 所有对外 API、Webhook 接口路径包含版本号：`/api/v1/analyze`
- 破坏性变更（字段删除、类型修改、必填变更、行为语义改变）必须升级版本号：`/api/v2/`
- 旧版本 API 至少保留 1 个迭代周期后下线，期间标记 `@deprecated` 并在响应头中返回 `Deprecation: true`
- 非破坏性新增（新增可选字段、新增端点）不升级版本号

### 20.2 数据模型兼容性

- 数据模型字段新增：可选字段（默认值），不影响现有逻辑
- **禁止**：删除或重命名已有核心字段、修改字段类型、改变字段语义
- 如有必要做不兼容变更，通过新增字段替代 + 旧字段标记废弃 + 下个版本移除的方式平滑过渡

### 20.3 回调签名机制

- 所有出站 Webhook 回调必须附带 `X-CodeGuard-Signature-256` 请求头
- 签名算法：`HMAC-SHA256(request_body, pre_shared_secret)`
- 接收方可校验回调请求真实性，防止伪造回调攻击
- 回调请求超时 5s，非 2xx 响应自动重试：1s / 3s / 10s / 30s / 60s（最多 5 次）
- 最终失败记录告警日志，消息不丢失

### 20.4 配置文件兼容性

- 配置文件新增项必须有默认值，保证旧配置可直接启动
- 配置项废弃：先标记 `@deprecated` + 打印警告日志 → 下个版本移除

---

> **文件版本**: v1.1  
> **创建时间**: 2026-08-03  
> **最后更新**: 2026-08-03  
> **变更记录**:  
> - v1.0: 初始版本，覆盖项目目标、开发原则、流程、架构、代码、测试、文档、Git、ADR、重构、专项规范、违规机制、规则维护、技术决策速查  
> - v1.1: 追加 5 个专项章节——安全链路专项规范（十六）、Agent 开发专项规范（十七）、供应链与依赖安全规范（十八）、文档目录结构规范（十九）、接口与兼容性规范（二十）
